<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE log4j:configuration PUBLIC "-//LOGGER" "log4j.dtd">

<log4j:configuration xmlns:log4j="http://jakarta.apache.org/log4j/">

	<appender name="myConsole" class="org.apache.log4j.ConsoleAppender">
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern"
				value="[%d{yyyy-MM-dd HH:mm:ss,SSS\}] %-5p [%t] %c{2\} - %m%n" />
		</layout>
		<!--过滤器设置输出的级别 -->
		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="levelMin" value="warn" />
			<param name="levelMax" value="error" />
			<param name="AcceptOnMatch" value="true" />
		</filter>
	</appender>

	<appender name="myFile" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="DatePattern" value="'.'yyyy-MM-dd'.log'" />
		<!-- 设置日志输出文件名 ,与tomcat属于同一路径 -->
		<param name="File" value="../resarvlogs/resarv_output${weblogic.Name}.log" />
		<!-- 设置是否在重新启动服务时，在原有日志的基础添加新日志 -->
		<param name="Append" value="true" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern"
				value="[RESARV][%d{yyyy-MM-dd HH:mm:ss,SSS\}] %p (%c:%L)- %m%n" />
		</layout>

		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="levelMin" value="warn" />
			<param name="levelMax" value="error" />
			<param name="AcceptOnMatch" value="true" />
		</filter>
	</appender>

	<appender name="wsHsuFile" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="DatePattern" value="'.'yyyy-MM-dd'.log'" />
		<!-- 设置日志输出文件名 ,与tomcat属于同一路径 -->
		<param name="File" value="../resarvlogs/hsu/resarv_output${weblogic.Name}.log" />
		<!-- 设置是否在重新启动服务时，在原有日志的基础添加新日志 -->
		<param name="Append" value="true" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern"
				value="[RESARV][%d{yyyy-MM-dd HH:mm:ss,SSS\}] %p (%c:%L)- %m%n" />
		</layout>

		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="levelMin" value="info" />
			<param name="levelMax" value="error" />
			<param name="AcceptOnMatch" value="true" />
		</filter>
	</appender>
	<!-- 任务.与hsu接口日志 -->
	<logger name="wsHsuFileLog" additivity="false">
		<level value="info" />
		<appender-ref ref="wsHsuFile" />
	</logger>

	<appender name="outServiceFile" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="DatePattern" value="'.'yyyy-MM-dd'.log'" />
		<!-- 设置日志输出文件名 ,与tomcat属于同一路径 -->
		<param name="File" value="../resarvlogs/common/resarv_output${weblogic.Name}.log" />
		<!-- 设置是否在重新启动服务时，在原有日志的基础添加新日志 -->
		<param name="Append" value="true" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern"
				value="[RESARV][%d{yyyy-MM-dd HH:mm:ss,SSS\}] %p (%c:%L)- %m%n" />
		</layout>

		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="levelMin" value="info" />
			<param name="levelMax" value="error" />
			<param name="AcceptOnMatch" value="true" />
		</filter>
	</appender>
	<!-- 接口服务日志 -->
	<logger name="outServiceFileLog" additivity="false">
		<level value="info" />
		<appender-ref ref="outServiceFile" />
	</logger>
	
	<appender name="dgmillServiceFile" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="DatePattern" value="'.'yyyy-MM-dd'.log'" />
		<!-- 设置日志输出文件名 ,与tomcat属于同一路径 -->
		<param name="File" value="../resarvlogs/dgmill/resarv_output${weblogic.Name}.log" />
		<!-- 设置是否在重新启动服务时，在原有日志的基础添加新日志 -->
		<param name="Append" value="true" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern"
				value="[RESARV][%d{yyyy-MM-dd HH:mm:ss,SSS\}] %p (%c:%L)- %m%n" />
		</layout>

		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="levelMin" value="info" />
			<param name="levelMax" value="error" />
			<param name="AcceptOnMatch" value="true" />
		</filter>
	</appender>
	<!-- 接口服务日志 -->
	<logger name="dgmillServiceFileLog" additivity="false">
		<level value="info" />
		<appender-ref ref="dgmillServiceFile" />
	</logger>


	<!-- 根logger的设置 -->
	<root>
		<priority value="warn" />
		<appender-ref ref="myConsole" />
		<appender-ref ref="myFile" />
	</root>

</log4j:configuration>